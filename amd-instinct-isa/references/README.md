# Local AMD Instinct ISA references

This directory intentionally contains no AMD PDFs in Git. Use
`../scripts/amd_instinct_isa.py download` after reviewing and accepting the
terms of each official document. Downloaded PDFs and the generated local
manifest are ignored by Git and must not be redistributed from this
repository.
