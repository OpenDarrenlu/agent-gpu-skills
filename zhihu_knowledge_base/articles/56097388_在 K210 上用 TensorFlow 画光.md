# 在 K210 上用 TensorFlow 画光

**作者**: SunnyCase​DL Compiler

**原文链接**: https://zhuanlan.zhihu.com/p/56097388

---

看了 Milo 大神的 用 C 语言画光系列 受益匪浅，就想能不能在 K210 上画光【x。无奈单片机资源都很有限，蒙特卡洛这种跑实时很难，思考能不能利用上 K210 的 AI 加速功能，于是就有了这篇文章。

本文的源码位于：sunnycase/k210_light2d

K210 是啥

K210 是一款包含 AI 硬件加速功能的双核 64bit RISC-V 芯片，它的运算能力可以胜任实时人脸识别。官网

1. 总体流程

输入圆的半径，输出发光圆形图像。

2. 准备数据集

在这里我用 C# 写了一段生成光追图像的代码，生成半径 [0, 0.6]，步长 0.1 的图像作为数据集。（和 Milo 大神的代码有点不同，没有用 ray marching）

// 判断光线有没有和椭圆相交（切）
static float Trace(Vector2 point, Vector2 lightDir)
{
    var m = point - _circle.Center;
    var c = Vector2.Dot(m, m) - MathF.Pow(_circle.Radius, 2);
    var b = Vector2.Dot(m, lightDir);
    var d = b * b - c;
    return d >= 0 ? 1.0f : 0;
}

// 求一个点收到的光强
static float Sample(Vector2 point, int seed)
{
    float sum = 0.0f;
    for (int i = 0; i < N; i++)
    {
        float a = TwoPI * (i + _rands[(seed + i) % _rands.Length]) / N;
        Vector2 dir = new Vector2(MathF.Cos(a), MathF.Sin(a));
        sum += Trace(point, dir);
    }
    return sum / N;
}


把这些图像保存成灰度 png，供训练使用。

static void Main(string[] args)
{
    var encoder = new PngEncoder { ColorType = PngColorType.Grayscale };
    foreach (var i in Enumerable.Range(0, 60))
    {
        var r = i / 120f;

        _circle.Radius = r;
        using (var img = new Image<Gray8>(W, H))
        {
            int seed = 0;
            for (int y = 0; y < H; y++)
            {
                Parallel.For(0, W, x =>
                {
                    ref var pixel = ref img.GetPixelRowSpan(y)[x];
                    var p = new Vector2((float)x / W, (float)y / H);

                    var value = (byte)MathF.Min(Sample(p, Interlocked.Increment(ref seed)) * 255.0f, 255.0f);
                    pixel = new Gray8(value);
                });
            }

            using (var fs = File.OpenWrite($"data/o_{i}.png"))
                img.SaveAsPng(fs, encoder);
        }
    }
}

数据集
3. 训练模型

模型很简单，从圆的半径开始上采样，生成 128x128 的灰度图。

def model(is_training):
    input = tf.placeholder(tf.float32, [None], name='input')

    out = tf.reshape(input, [-1,1,1,1])
    out = deconv2d(out, 128, 2, is_training, True)
    out = deconv2d(out, 64, 1, is_training, True)
    out = deconv2d(out, 32, 2, is_training, True)
    out = deconv2d(out, 16, 2, is_training, True)
    out = deconv2d(out, 8, 2, is_training, True)
    out = deconv2d(out, 4, 2, is_training, True)
    out = deconv2d(out, 2, 2, is_training, True)
    out = deconv2d(out, 1, 2, is_training, False)
    out = tf.identity(out, name='output')
    return (input, out)
4. 在 K210 上运行

K210 支持 TFLite 模型的 inference，所以我们先把模型转换成 TFLite 的格式。

toco --graph_def_file=basic.pb --output_file=basic.tflite --output_format=TFLITE --input_shape=1 --input_array=input --output_array=output --inference_type=FLOAT

然后调用 TFLite 的 API，让输入在 0 到 0.5 之间变化，并把输出的图像画到 LCD 上。

// Load the model
std::unique_ptr<tflite::FlatBufferModel> model = tflite::FlatBufferModel::BuildFromBuffer((const char *)model_data, model_size);

// Build the interpreter
NeededOpResolver resolver;
InterpreterBuilder builder(*model.get(), resolver);
std::unique_ptr<Interpreter> interpreter;
builder(&interpreter, 1);
TFLITE_MINIMAL_CHECK(interpreter != nullptr);
// Allocate tensor buffers.
TFLITE_MINIMAL_CHECK(interpreter->AllocateTensors() == kTfLiteOk);

// Fill input buffers
int input = interpreter->inputs()[0];
int i = 0;
int step = 1;
while (1)
{
    i = i + step;
    if (i >= 5 || i <= 0)
        step = -step;
    interpreter->typed_tensor<float>(input)[0] = i / 10.f;
    // Run inference
    TFLITE_MINIMAL_CHECK(interpreter->Invoke() == kTfLiteOk);
    auto output_it = interpreter->typed_output_tensor<float>(0);
    for (size_t i = 0; i < 128 * 128; i++)
    {
        auto gray = std::clamp((int)(output_it[i] * 255), 0, 255);
        auto rgb = ((gray & 0b11111000) << 8) | ((gray & 0b11111100) << 3) | (gray >> 3);
        size_t d_i = i % 2 ? (i - 1) : (i + 1);
        lcd_gram[d_i] = rgb;
    }
    lcd_draw_picture(100, 60, 128, 128, reinterpret_cast<uint32_t*>(lcd_gram));
    vTaskDelay(5);
}

5. 最终效果
00:02
在 K210 上用 TensorFlow 画光

希望大家能喜欢这篇文章，有兴趣也可以来玩玩 K210（<ゝω・）～☆Ｋｉｒａ
