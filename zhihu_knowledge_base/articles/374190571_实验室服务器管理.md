# 实验室服务器管理

**作者**: 工藤福尔摩MLSys

**原文链接**: https://zhuanlan.zhihu.com/p/374190571

---

由于实验室新买了服务器，我做了一个服务器管理平台。有需要的同学可以参考

ServerManager
github.com/Maximilianxu/server_management

基本效果如下

后台如何分配实例

使用的是lxd做容器化，比较轻量，非常方便管理员自动或者手动管理。

常用的lxd命令参考lxd文档和我代码中的util.py。

用户如何使用

申请成功后，会给相应的实例一个端口和随机密码，用户通过ssh连接即可。

Features
实例拥有root权限
实例可以独占GPU
每一个实例都可以动态添加GPU，回收GPU
包含了配额制，用户最多申请多少实例或者多少GPU
GPU数量不足时，会进入排队，等待其他实例释放GPU
Dependencies
后端

1. MongoDB

2. pymongo

3. Flask

前端

1. 安装 nodejs 14.x

2. 安装各种依赖: 进入client目录, yarn & yarn install

运行
Client

npm run serve

Server

python3 app.py

Ref Manual，一些LXD的tips
inst内的torch无法访问cuda
#原因不明，第一次恢复是
sudo lxc profile edit default
#在config下面加入这一行  
nvidia.driver.capabilities: all
# 然后重启inst

第二次恢复则是把这一行删掉，重启inst。推测是修改profile之后，会重新读取，自然而然就能恢复访问cuda。和怎么改关系不大。

不过还有可能实例重启一段时间之后才能正常访问cuda。

网站崩溃
# 是否是mongodb崩溃

sudo systemctl status mongod

# 如果没在运行，尝试

sudo systemctl start mongod

# 如果启动失败，尝试，一般可能在机房断电重启之后发生

sudo chown mongodb:mongodb /tmp/mongodb-27017.sock

sudo chown -R mongodb:mongodb /var/lib/mongodb

容器很卡的时候

考虑是否是空间不足

每一个容器的配置文件都是用的default profile

查看/修改default（不建议）

sudo lxc profile show/edit default # 默认64G一个instance

另外，也能看到所有inst的配置文件里都包含了名为root的device，该device就是root file system，所有inst都是从名为zfs-pool的池子中分配的。

在某容器中执行

df -lh

也能看到该容器对zfs-pool的使用情况

暂时添加空间

lxc stop cbuild service 

sudo truncate -s +10G /var/snap/lxd/common/lxd/disks/lxd.img # 加10G到存储池

sudo zpool set autoexpand=on zfs-pool

sudo zpool online -e zfs-pool /var/snap/lxd/common/lxd/disks/lxd.img

sudo zpool set autoexpand=off zfs-pool 

sudo snap start lxd


一劳永逸的扩容

sudo lxc storage info zfs-pool # 查看zfs-pool的使用情况，目前的分区是zfs-pool占用了/dev/sda下的1号分区;
lsblk # 查看所有分区

基本的做法就是扩大/dev/sda分区，使用工具cfdisk

sudo cfdisk /dev/sda # 在里面选择new size，之后write(保存)，quit即可

设置zfs-pool 自动扩张充满sda1

sudo zpool set autoexpand=on zfs-pool
sudo zpool online -e zfs-pool /dev/sda1

那整个sda都用光了怎么办？(几乎不可能发生)。添加硬盘，参考这里

https://www.theshell.guru/how-to-increase-the-size-of-a-zfs-pool-add-drive-to-zfs-pool/

提升inst的硬盘大小

我直接放我自己用的脚本好了。

for i in {0..10..1}
do
        echo "improve inst$i"
        sudo lxc config device set inst$i root size=256GB
done
network not available

非DNS问题

现象：

1. 可以ping host server，比如ping 210.28.xxx.xx，这是我们服务器IP

2. dns works，ping baidu.com，会显示ping 百度的ip

可能是防火墙问题：

sudo iptables -P FORWARD ACCEPT

DNS问题

问题比较复杂，参考。

Failed to run: dnsmasq - Address already in use

check 端口53

sudo netstat -lnp | grep ":53 "

我当时应该停掉了dnsmasq，安装了systemd-resolved。具体细节记不清了，systemd-resolved可能依赖dnsmasq-base。

目前我的"systemctl status dnsmasq"

会显示dnsmasq inactive。

但是" sudo netstat -lnp | grep ":53 "

会显示存在一个dnsmasq监听了10.176.xxx.xxx。这个IP是lxd instance的虚拟IP段，可以通过lxd list确认。我猜测是lxd里包含了一个dnsmasq服务用于转发lxd instances的dns请求，之后这个请求被转发到宿主systemd-resolved服务那里。因为systemd-resolved监听了127.0.0.1:53地址。but not sure。
