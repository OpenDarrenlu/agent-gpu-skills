# 《统计学习方法》：朴素贝叶斯

**作者**: cherichy宁做我

**原文链接**: https://zhuanlan.zhihu.com/p/58997595

---

朴素贝叶斯法是最简单的分类方法之一，其对训练数据有一定的要求，即各特征向量 
𝑋
 及 
𝑌
 的取值须在一有限集合内，且各维的特征的条件概率相对独立。这是一个比较强的假设，故称为朴素贝叶斯。

贝叶斯公式

𝑃
(
𝐴
𝐵
)
=
𝑃
(
𝐴
|
𝐵
)
𝑃
(
𝐵
)
=
𝑃
(
𝐵
|
𝐴
)
𝑃
(
𝐴
)

独立性假设

𝑃
(
𝑋
=
𝑥
|
𝑌
=
𝑦
𝑗
)
=
∏
𝑖
=
1
𝑛
𝑃
(
𝑋
=
𝑥
𝑖
|
𝑌
=
𝑦
𝑗
)

将二者结合起来，我们就能根据训练数据计算出先验概率，再根据贝叶斯公式计算后验概率了

𝑃
(
𝑌
=
𝑦
𝑗
|
𝑋
=
𝑥
)
=
𝑃
(
𝑋
=
𝑥
|
𝑌
=
𝑦
𝑗
)
𝑃
(
𝑌
=
𝑦
𝑗
)
𝑃
(
𝑋
=
𝑥
)
=
𝑃
(
𝑋
=
𝑥
|
𝑌
=
𝑦
𝑗
)
𝑃
(
𝑌
=
𝑦
𝑗
)
Σ
𝑗
𝑃
(
𝑋
=
𝑥
|
𝑌
=
𝑦
𝑗
)
𝑃
(
𝑌
=
𝑦
𝑗
)

然后将独立性假设的公式带入，分类结果取后验概率最大的那一类即可。而且由于分母对于一次估计来说都是一样的，故仅需比较分子的大小即可。

贝叶斯估计

贝叶斯估计核心思想就是给每个频数加上一个正数 
𝜆
 ，这样可以消除极大似然估计中可能出现的概率估计值为0的情况。实现仅需统计频数时加上 
𝜆
 即可。

公式非常的简单，实现思路也很直接。首先得统计训练数据中 
𝑋
𝑖
 及 
𝑌
 的集合，然后统计
𝑃
(
𝑌
=
𝑦
𝑗
)
 及每个 
𝑋
𝑖
 集合中对应的条件概率。由于各集合的取值可以为任意标签，而不局限于数值，故还需建立一个字典进行索引。但是若要从头实现贝叶斯，其中的细节还是比较烦人的。

在我的实现中采用了Pandas完成其中的统计操作，主要用到了pd.crosstab 这一函数来完成各频率的统计。

class NaiveBayes:
    def __init__(self, data, lam=0):
        df = pd.DataFrame(data)
        dim = df.shape[1]
        self.y_p = df[dim-1].groupby(df[dim-1]).count()+lam
        self.y_p /= self.y_p.sum()
        self.cb = []
        for i in range(dim-1):
            xi_p = pd.crosstab(df[i], df[dim-1])+lam
            self.cb.append(xi_p/xi_p.sum())

而估计也很简单，对每个维度分别计算后验概率 P(X=x_i|Y=y_j) ，累乘，再乘上 P(Y=y_j) 便完成了对一个label的估计。

def predict(self, x):
    labels = self.y_p.index
    res = pd.Series(np.zeros(labels.size), index=labels)
    for y in labels:
        res[y] = self.y_p[y]
        for i in range(len(x)):
            try:
                res[y] *= self.cb[i][y][x[i]]
            except KeyError as e:
                print('KeyError!', e)
                return None
    print(res)
    return res.idxmax()

感谢Pandas，极大地缩短了我实现朴素贝叶斯的代码行数，实在是做统计的利器（笑出声

完整代码见Github，欢迎star哦~
