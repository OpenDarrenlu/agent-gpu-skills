# 《统计学习方法》：KNN（kd树实现）

**作者**: cherichy宁做我

**原文链接**: https://zhuanlan.zhihu.com/p/59041711

---

KNN（K-nearest neighbor）的基本思想非常的简单朴素，即对于一个待预测的样本 
𝑥
 ，在训练集中找到距离其最近的 
𝑘
 个近邻 ，得票最高的类作为输出类别即可。当 
𝑘
=
1
 时，则称为最近邻。OK，到这里原理就说完了，没错就是这么简单。

Naive实现

最简单的实现仅需要遍历所有的点，按距离排序取前k个即可，简直美滋滋。

import numpy as np
# x is the data to be classified.
# X is the train data
# p is the norm parameter
KNN=sorted(X,key=lambda xi:np.linalg.norm(x-xi,p))[:k]

注释都比代码长啊有没有。。但是这么朴素的想法其复杂度也显而易见，每一次查询或预测都必须和所有的点比，即复杂度为 
𝑂
(
𝑛
)
 。当待查询点数量极多时，这一开销是不可接受的。

kd树

为了加速查询点的近邻，我们必须利用好原始数据的性质。我们知道，在一个排好序的一维序列中寻找距离目标最近的实例的复杂度仅为 
𝑂
(
log
⁡
𝑛
)
 ，远低于一次遍历的开销。另外，对无序序列进行排序或者构建二叉查找树的开销也仅需 
𝑂
(
𝑛
log
⁡
𝑛
)
 ，且仅需一次构建即可。在高维数据中也存在着类似的数据结构，能对高维数据集进行较快的查找和定位，即kd树（k-dimensional tree）。

kd树的构建

kd树的构建规则很巧妙，每次取一个维度的中位数为划分点，将其作为树的节点，将数据集分成左右两部分递归地进行划分。值得注意的是，下一层划分的维度要和上一层不同，一般来说直接选择模 
𝑘
 意义下的下一个维度。

为了实现kd树的构建，首先需要定义kd树的节点，根据构建的过程，其应该包含自己的数据，左右子树，以及当前切分维度等信息。

class Node:
    def __init__(self, data, sp=0, left=None, right=None):
        self.data = data
        self.sp = sp
        self.left = left
        self.right = right

构建过程即是一个递归的过程，这里排序实际上仅需要 k 次，但我这里排序了 n 次。于是复杂度就从 O(k*n\log n) 变成了 O(n^2\log n) emmmm。。不过为了简单就先这样吧，将会在下一版改进，标个TODO先。

class KDTree:
    def __init__(self, data):
        k = data.shape[1]
        def create(dataset, sp):
            if len(dataset) == 0:
                return None
            # sort by current dimension
            dataset = sorted(dataset, key=lambda x: x[sp])
            mid = len(dataset)//2
            # split by the median
            dat = dataset[mid]
            return Node(dat, sp, create(dataset[:mid], (sp+1) % k),\
                                 create(dataset[mid+1:], (sp+1) % k))
        self.root = create(data, 0)

kd树搜索算法

在kd树中搜索一个节点和在二叉搜索树中极其类似。

如果当前维度的值节点小则转左，大则转右，进行下一个维度的搜索，将最终得到的叶子节点设为当前最近点。
和二叉搜索树不同，kd树的查找还需要一个递归回退的过程。从当前最近点开始，依次回退并检查兄弟节点是否存在有比当前最近点更近的点。这一操作仅需比较目标点和分离超平面的距离和当前最近的距离即可，若前者小，则有可能存在更近点，须递归访问，直到最后回退到根节点即完成了搜索。

多谢递归，算法看起来很复杂，但是实现起来非常简单。

def nearest(self, x, near_k=1, p=2):
    # use the max heap builtin library heapq
    # init the elements with -inf, and use the minus distance for comparison
    # the top of the max heap is the min distance.
    self.knn = [(-np.inf, None)]*near_k
    def visit(node):
        if not node == None:
            # cal the distance to the split point, i.e. the hyperplane
            dis = x[node.sp] - node.data[node.sp]
            # visit the child node recursively
            # if returned, we get the current nearest point
            visit(node.left if dis < 0 else node.right)
            # cal the distance to the current nearest point
            curr_dis = np.linalg.norm(x-node.data, p)
            # push the minus distance to the heap
            heapq.heappushpop(self.knn, (-curr_dis, node))
            # compare the distance to the hyperplane with the min distance
            # if less, visit another node.
            if -(self.knn[0][0]) > abs(dis):
                visit(node.right if dis < 0 else node.left)
    visit(self.root)
    self.knn = np.array(
        [i[1].data for i in heapq.nlargest(near_k, self.knn)])
    return self.knn

完整代码可见Github，希望能喜欢~~
